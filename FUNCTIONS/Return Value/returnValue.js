//Write a simple function multiply which accepts two numerical 
//arguments and returns their product (multiply them together).  
//Make sure to return the value instead of printing it.


function multiply(x,y) {
    return (x*y);
}

